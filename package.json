{
  "name": "authcore",
  "private": true,
  "version": "0.5.1",
  "description": "Devise-inspired, framework-agnostic authentication library for the JS ecosystem",
  "scripts": {
    "build": "pnpm --filter @authcore/core build && pnpm --filter @authcore/prisma-adapter build && pnpm --filter @authcore/resend-adapter build && pnpm --filter @authcore/nodemailer-adapter build && pnpm --filter @authcore/express build && pnpm --filter @authcore/fastify build && pnpm --filter @authcore/react build && pnpm --filter create-authcore-app build",
    "publish:all": "pnpm -r publish --access public",
    "publish:dry": "pnpm -r publish --access public --dry-run",
    "dev": "pnpm --recursive --parallel dev",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "lint": "tsc --noEmit",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  }
}
